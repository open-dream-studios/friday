# Scope

The scope document for this job is the uploaded file **scope.pdf** (attached to DB job JOB-01KVZ3MRF1455VKMH8Y22P50E4). The brain trunk's hydration loop extracts its text to `inputs/files/scope.pdf.txt` — read THAT for the full scope content.

If `inputs/files/scope.pdf.txt` is missing, the hydration consumer hasn't finished extracting yet — wait a few seconds and try again, or check server_a1 logs.
