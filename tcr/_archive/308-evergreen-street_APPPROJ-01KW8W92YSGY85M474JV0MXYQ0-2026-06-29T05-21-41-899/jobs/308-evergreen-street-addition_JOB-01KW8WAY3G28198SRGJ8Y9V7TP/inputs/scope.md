# Scope

The scope document for this job is the uploaded file **scope.pdf** (attached to DB job JOB-01KW8WAY3G28198SRGJ8Y9V7TP). The brain trunk's hydration loop extracts its text to `inputs/files/scope.pdf.txt` — read THAT for the full scope content.

If `inputs/files/scope.pdf.txt` is missing, the hydration consumer hasn't finished extracting yet — wait a few seconds and try again, or check server_a1 logs.
