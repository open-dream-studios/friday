# `job_types/bathroom_remodel/` — bathroom-remodel-specific knowledge

Rules + beliefs for interior bathroom remodels (no new footprint).

## Subfolders

- `rules/` — bathroom-remodel-specific prescriptive rules.
- `beliefs/` — bathroom-remodel-specific descriptive beliefs.
- `reference_jobs/` — hand-curated pointers to past bathroom remodels.

See `friday/tcr/job_types/README.md` for the file-shape contracts and
the authority-hierarchy rule.

## Empty for now

Populated as beliefs/rules accumulate. The folder exists so the slot is
visible and the agent knows it's a recognized job type.
